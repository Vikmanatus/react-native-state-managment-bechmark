import React from 'react';
import {SafeAreaView, Text} from 'react-native';

export function ApolloOnly(): React.JSX.Element {
  return (
    <SafeAreaView style={{flex: 1}}>
      <Text>Appollo only</Text>
    </SafeAreaView>
  );
}
